[package]
name = "monte"
version = "0.0.1"
edition = "2024"

[profile.release]
strip = true
opt-level = 3
lto = true
codegen-units = 4

[dependencies]
codespan-reporting = "0.13.1"
klystron = { path = "../klystron" }
typesys = { path = "../typesys" }